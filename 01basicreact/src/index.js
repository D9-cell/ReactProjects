import React from 'react'; /* Core Foundational Library which takes references*/
import ReactDOM from 'react-dom/client'; /* REACT DOM is implementation of REACT*/
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); /*React DOM is a virtual DOM created by US that connects with browser DOM */
root.render(
  
    <App />
  
);


